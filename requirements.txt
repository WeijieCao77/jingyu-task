# --- Voice agent runtime (telephony pipeline) ---
livekit-agents[silero,turn-detector]~=1.5
livekit-plugins-openai~=1.5
livekit-plugins-anthropic~=1.5
livekit-plugins-silero~=1.5
livekit-api>=1.0          # mint join tokens for the in-browser voice client

# Windows lacks the IANA tz DB that zoneinfo (Asia/Shanghai) needs at runtime.
tzdata; sys_platform == "win32"

# Pin to versions compatible with livekit-plugins-turn-detector 1.5.x
# (transformers 5.x / huggingface_hub 1.x break the turn-detector model load;
#  the agent still degrades to VAD-only, but these keep turn detection working).
transformers>=4.40,<5
huggingface_hub>=0.23,<1

# --- LLM / STT / TTS SDKs (also used by the offline simulator) ---
anthropic>=0.69
openai>=2.0

# --- Notification + web confirm endpoint ---
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27

# --- Data / config ---
SQLAlchemy>=2.0
psycopg2-binary>=2.9      # Postgres driver for cloud DB (Neon); SQLite needs nothing
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# --- Company roster matching (optional: better homophone matching) ---
pypinyin>=0.51

# --- Dev / test ---
pytest>=8.0
