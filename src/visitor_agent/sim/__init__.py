"""Offline conversation simulator (text in / text out, no telephony, no audio).

Lets you validate the *conversation quality and slot extraction* with only an
Anthropic key — before wiring up Twilio/LiveKit. It runs the exact same system
prompt, tools, and RegistrationSession the phone call uses.
"""
