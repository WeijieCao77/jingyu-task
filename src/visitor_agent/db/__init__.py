"""Persistence layer (SQLite by default, any SQLAlchemy URL via DATABASE_URL).

Visit records power three things: the guard-confirm flow, returning-visitor
recognition, and the guard query agent.
"""
